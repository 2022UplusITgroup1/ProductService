server:
  port: 64421

spring:
  application:
    name: ProductService

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka